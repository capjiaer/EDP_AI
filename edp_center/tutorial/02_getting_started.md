# 快速开始

[← 返回目录](../TUTORIAL.md)

本指南将帮助你创建第一个项目并运行第一个命令。

## 假设你的环境

假设你的路径为：
- **edp_center 路径**: `/home/user/EDP_AI/edp_center` (Linux) 或 `C:/Users/username/Desktop/EDP_AI/edp_center` (Windows)
- **WORK_PATH 路径**: `/home/user/WORK_PATH` (Linux) 或 `C:/Users/username/WORK_PATH` (Windows)

---

## 第一步：创建第一个项目

### 方式 A: 使用 GUI 图形界面（推荐新手）

```bash
# 1. 进入工作路径目录
cd /home/user/WORK_PATH  # Linux
# 或
cd C:/Users/username/WORK_PATH  # Windows

# 2. 启动 GUI 初始化界面
edp_init -init --gui
```

然后按照 GUI 界面提示填写信息：
1. **选择 EDP Center 路径和 Work Path 路径**
   - EDP Center 路径：选择 EDP Center 资源库的路径
   - Work Path 路径：**如果当前目录在已初始化的项目下，GUI 会自动检测并设置正确的 WORK_PATH 根目录**（通过查找 `.edp_version` 文件）
   - 也可以手动浏览选择路径
2. **选择项目**：从下拉列表选择项目（格式：`项目名 (foundry/node)`）
   - 如果项目列表为空，可以点击"刷新"按钮手动刷新
   - 项目列表从 EDP Center 的 `config/` 目录扫描
3. **输入项目版本**：输入项目版本（如 `P85`）
4. **配置 Block 和 User**：
   - 在表格中添加 Block 和 User 配置
   - 支持添加/删除行
   - 多个 User 用逗号分隔
5. **可选操作**：
   - 点击"从配置文件加载"：从 `config.yaml` 文件加载参数
   - 点击"自动推断参数"：从 `.edp_version` 文件或路径自动推断参数
6. **开始初始化**：点击"开始初始化"按钮

### 方式 B: 使用命令行

```bash
# 1. 进入工作路径目录
cd /home/user/WORK_PATH  # Linux
# 或
cd C:/Users/username/WORK_PATH  # Windows

# 2. 创建 config.yaml 文件
cat > config.yaml << 'EOF'
project:
  name: dongting          # 项目名称
  version: P85            # 项目版本
  blocks:                 # 块和用户列表
    block1: user1 user2   # block1 包含 user1 和 user2
    block2: user3 user4   # block2 包含 user3 和 user4
EOF

# 3. 执行初始化
edp_init -init
```

**初始化结果**：

执行 `edp_init -init` 后，会在 WORK_PATH 下创建以下目录结构：

```
/home/user/WORK_PATH/              # 或 C:/Users/username/WORK_PATH/
└── dongting/                      # 项目名称
    └── P85/                       # 项目版本
        ├── .edp_version           # 项目版本信息文件
        ├── block1/                # 块1
        │   ├── user1/             # 用户1
        │   │   └── main/          # 默认分支（自动创建）
        │   │       ├── cmds/      # 命令脚本目录（生成的 Tcl 脚本）
        │   │       ├── data/      # 数据目录（数据文件，用于 RELEASE）
        │   │       ├── hooks/     # Hooks 目录（自定义 hooks）
        │   │       ├── runs/      # 运行目录（运行时临时文件）
        │   │       ├── user_config.tcl   # 用户配置文件（TCL格式）
        │   │       ├── user_config.yaml  # 用户配置文件（YAML格式）
        │   │       └── .run_info  # 运行信息文件（运行后生成）
        │   │       # 注意：logs/ 和 rpts/ 会在运行时自动创建
        │   └── user2/             # 用户2
        │       └── main/          # 默认分支
        └── block2/                # 块2
            ├── user3/
            │   └── main/
            └── user4/
                └── main/
```

**恭喜！** 你已经完成了第一个项目的初始化。

---

## 第二步：运行第一个命令

现在让我们运行第一个流程命令：

```bash
# 1. 进入分支目录
cd /home/user/WORK_PATH/dongting/P85/block1/user1/main
# 或
cd C:/Users/username/WORK_PATH/dongting/P85/block1/user1/main

# 2. 运行一个步骤
edp -run pv_calibre.ipmerge
```

**执行过程**：

执行 `edp -run pv_calibre.ipmerge` 时，框架会自动：

1. **加载配置**：合并所有配置文件，生成 `runs/pv_calibre/ipmerge/full.tcl`
2. **处理脚本**：展开 `#import` 指令，插入 hooks，生成 `cmds/pv_calibre/ipmerge.tcl`
3. **执行命令**：根据配置执行本地或 LSF 命令
4. **记录信息**：更新 `.run_info` 文件

---

## 第三步：查看结果

### 查看运行日志

```bash
# 查看日志文件
cat logs/pv_calibre/ipmerge/pv_calibre_ipmerge.log
```

### 查看运行状态

```bash
# 查看运行信息文件
cat .run_info
```

### 查看生成的脚本

```bash
# 查看最终生成的脚本
cat cmds/pv_calibre/ipmerge.tcl

# 查看合并后的配置
cat runs/pv_calibre/ipmerge/full.tcl
```

---

## 演示模式（不实际执行）

如果你想先看看会执行什么命令，可以使用 `--dry-run` 模式：

```bash
# 只显示构建的命令，不实际执行
edp -run pv_calibre.ipmerge --dry-run
```

这会显示完整的命令，包括工作目录、日志文件路径等，方便你验证配置是否正确。

---

## 下一步

- 📖 [学习基本使用方法](03_basic_usage.md)
- 🔧 [了解高级功能](04_hooks_and_imports.md)
- ❓ [查看常见问题](08_faq.md)

[← 返回目录](../TUTORIAL.md)

